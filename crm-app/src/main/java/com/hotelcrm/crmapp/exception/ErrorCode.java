package com.hotelcrm.crmapp.exception;

public enum ErrorCode {
    NOT_FOUND,
    VALIDATION_FAILED,
    AUTHENTICATION_FAILED,
    ACCESS_DENIED,
    CONFLICT,
    BAD_REQUEST,
    INTERNAL_ERROR,
    JWT_INVALID,
    JWT_EXPIRED
}
